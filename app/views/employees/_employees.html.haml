-@employees.each do |employee|
	%div.employee
		%span.name
			= link_to employee.fullname, employee_path(employee), :class => "employee-details"
		%span.extension
			=employee.extension
		%span.department
			=employee.title
