-@employees.each do |employee|
	%div.employee{:url=> company_employee_path( @company, employee ) }
		.name
			= employee.fullname
		%span.extension
			= employee.extension
		%span.function
			= employee.title