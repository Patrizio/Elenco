-@employees.each do |employee|
	%div.employee{:href => company_employee_path( @company, employee ) }
		.name
			= employee.fullname
		%span.extension
			= employee.extension
		%span.function
			= employee.title