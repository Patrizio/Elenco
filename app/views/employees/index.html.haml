 
%h1 Alle medewerkers

= form_tag employees_path, :method => 'get', :id => "employees_search" do
	%p
		= text_field_tag :search, params[:search], :type => 'search'
		= submit_tag t(:search_label), :name => nil

%div#employees
	= render 'employees'